# Agent Context — feat: Obsidian Vault Integration Plugin v2

*Generated: 2026-05-16T21:46:45.748691 | Task ID: bbf74577-bd7-v2 | Workspace: /Users/brassfieldventuresllc/.hermes/.worktrees/agent/task-bbf74577-bd7-v2-feat--obsidian-vault-integration-plugin-*

## Task Spec

**Title:** feat: Obsidian Vault Integration Plugin v2
**Priority:** 3
**Status:** running
**Workspace Kind:** worktree

**Body:**
Write a Hermes plugin that connects to a local Obsidian vault. It should expose a tool called search_obsidian that takes a query, searches markdown files in the vault directory, and returns the top matches with file path and relevant snippet.

CRITICAL: Write plugin files to ./hermes-agent/plugins/obsidian-sync/ INSIDE this worktree directory (not to ~/.hermes/plugins/). The plugins directory inside this worktree is a git worktree — your files WILL be diffed and merged by the Judge Gate. Run  after writing files.

Requirements:
- plugin.yaml with name, version, description
- plugin_api.py with FastAPI router + register(ctx) function
- Tool: search_obsidian(query: str, vault_path: str = ~/vault, limit: int = 5)
- Search recursively through all .md files
- Return top matches sorted by relevance score

## Historical Context (from G-Brain)

No results.

## CLAUDE.md Rules

(No CLAUDE.md found in repo root)

## Fleet Conventions

- Prefix commits: `[agent:plugin-builder:bbf74577-bd7-v2]`
- Test before declaring complete
- On completion: write result to kanban.db + emit task_event kind=completed
- On failure: archive state to G-Brain before cleanup

## Workspace Lifecycle

- **Ephemeral**: wiped immediately on task completion/termination
- **Worktree**: kept until merged/archived, pruned after 72h stale
- **Scratch**: deleted on task completion

## 🚨 CRITICAL: Path Boundary Rules

**This is a ISOLATED WORKTREE. All file writes MUST stay inside this directory.**

- ✅ Write to `./plugins/<name>/`, `./skills/<name>/`, `./tools/`, `./hermes-agent/plugins/`
- ✅ Use RELATIVE paths: `./`, `./plugins/`, `./hermes-agent/plugins/`
- ❌ NEVER use `~/.hermes/` or absolute paths like `/Users/brassfieldventuresllc/.hermes/`
- ❌ NEVER write to the host machine's ~/.hermes/plugins/ directory
- ❌ NEVER write to the host machine's ~/.hermes/hermes-agent/ directory

**CRITICAL — Plugin writers:** Hermes plugins live at `./hermes-agent/plugins/<name>/` (relative to this directory). Write plugin files there, e.g. `./hermes-agent/plugins/obsidian-sync/plugin.yaml`. After writing, run `git add . && git commit -m "[agent:plugin-builder:bbf74577-bd7-v2] feat: <short description>"` so your changes appear in the git diff. An empty diff will FAIL the Judge Gate.

**The directory you are in IS the git worktree. Your git branch is `agent/task-bbf74577-bd7-v2-feat--obsidian-vault-integration-plugin-`.**
Files you write here will be diffed and merged. Files written outside will be invisible to the Judge Gate.

## Next Steps

1. Read task body fully
2. Query G-Brain for related patterns if needed
3. Execute task — write all output files relative to this directory
4. On success: update kanban.db status=DONE, emit task_event
5. On failure: archive logs to G-Brain, update consecutive_failures
